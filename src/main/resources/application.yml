server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/paul_springboot
    username: postgres
    password: Sam941226!
  jpa:
    hibernate:
      ddl-auto: 'update'
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: SERVLET